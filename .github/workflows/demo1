name: First Demo

on:
  workflow_dispatch:

Jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@4
      
      - name: Build
        run: echo "Build Code"
